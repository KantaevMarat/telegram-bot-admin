# Обновление системы уведомлений о балансе

## Краткое описание

Реализована полная система уведомлений пользователям Telegram-бота при изменении баланса, включая отображение причины изменения и автоматическое обновление fake-статистики.

## ✅ Реализованные функции

### 1. Система уведомлений в Telegram

**Файл:** `backend/src/modules/bot/bot.service.ts`

Добавлен новый метод `sendBalanceChangeNotification()`, который отправляет пользователю красиво оформленное уведомление с:
- Типом операции (пополнение/списание)
- Суммой изменения
- Балансом до и после операции
- Причиной изменения
- Датой и временем операции

**Особенности:**
- Автоматический перевод стандартных кодов причин на русский язык
- Обработка ошибок (блокировка бота пользователем, недоступность чата)
- Не блокирует основную транзакцию при ошибках отправки

### 2. Интеграция с UsersService

**Файл:** `backend/src/modules/users/users.service.ts`

Обновлены методы:
- `updateBalance()` - ручное изменение баланса администратором
- `createPayoutRequest()` - создание заявки на вывод средств

Для каждого изменения баланса теперь:
- ✅ Записывается транзакция в `balance_logs`
- ✅ Отправляется уведомление пользователю
- ✅ Обновляется fake-статистика
- ✅ Логируется в консоль

### 3. Полное логирование в bot.service.ts

**Файл:** `backend/src/modules/bot/bot.service.ts`

Добавлено логирование и уведомления для всех операций с балансом:
- `giveReferralBonus()` - реферальные бонусы
- `submitTask()` - награды за автоодобренные задания
- `handleTaskVerification()` - награды за верифицированные задания

### 4. Автоматическое обновление fake-статистики

При каждом изменении баланса автоматически вызывается `fakeStatsService.regenerateFakeStats()` для актуализации показателей в админ-панели.

## 📝 Структура уведомления

Пример уведомления пользователю:

```
💰 Пополнение баланса

💵 Сумма: +50.00 USDT
📊 Было: 100.00 USDT
📈 Стало: 150.00 USDT

📝 Причина: Пополнение администратором
📅 Дата: 04.11.2025, 15:30
```

## 📊 Логирование в balance_logs

Каждое изменение баланса записывается в таблицу `balance_logs` со следующими полями:

- `user_id` - ID пользователя
- `admin_tg_id` - Telegram ID администратора (если применимо)
- `delta` - Сумма изменения (положительная/отрицательная)
- `balance_before` - Баланс до изменения
- `balance_after` - Баланс после изменения
- `reason` - Код причины изменения
- `comment` - Комментарий или описание операции
- `created_at` - Дата и время операции

## 🔧 Технические детали

### Коды причин (reason)

Поддерживаемые коды:
- `manual_adjustment` - Ручная корректировка администратором
- `admin_add` - Пополнение администратором
- `admin_deduct` - Списание администратором
- `task_reward` - Награда за выполнение задания
- `referral_bonus` - Реферальный бонус
- `payout_request` - Заявка на вывод средств
- `payout_rejected` - Отклонение заявки на вывод
- `payout_completed` - Завершение вывода средств

### Обработка ошибок

Все уведомления отправляются асинхронно (без await) и не блокируют основные операции. При ошибках:
- Логируется предупреждение в консоль
- Транзакция с балансом НЕ откатывается
- Обрабатываются специфические ошибки Telegram API:
  - `403` - пользователь заблокировал бота
  - `chat not found` - чат не найден

### Модульная структура

Обновлены зависимости модулей:
- `UsersModule` → импортирует `BotModule` и `StatsModule`
- `BotModule` → экспортирует `BotService`
- `StatsModule` → экспортирует `FakeStatsService`

Используется `forwardRef()` для предотвращения циклических зависимостей.

## 🚀 Как использовать

### Через админ-панель

1. Откройте страницу "Пользователи"
2. Найдите нужного пользователя
3. Нажмите "Изменить баланс"
4. Укажите сумму (положительную для пополнения, отрицательную для списания)
5. Укажите причину в поле "Комментарий"
6. Сохраните

Пользователь автоматически получит уведомление в Telegram.

### Программно

```typescript
// Через UsersService
await this.usersService.updateBalance(
  tgId,           // Telegram ID пользователя
  50,             // Сумма изменения
  'admin_add',    // Код причины
  adminTgId,      // Telegram ID админа (опционально)
  'Бонус за активность', // Комментарий (опционально)
);
```

## ✅ Проверка работы

После изменения баланса пользователя через админ-панель:

1. ✅ В Telegram-боте пользователь получает уведомление
2. ✅ В таблице `balance_logs` появляется запись
3. ✅ Fake-статистика обновляется
4. ✅ В консоли backend появляются логи операции

## 📋 Измененные файлы

1. `backend/src/modules/bot/bot.service.ts` - добавлен метод отправки уведомлений
2. `backend/src/modules/bot/bot.module.ts` - добавлен импорт `BalanceLog`
3. `backend/src/modules/users/users.service.ts` - интеграция уведомлений и статистики
4. `backend/src/modules/users/users.module.ts` - добавлены зависимости
5. `backend/src/modules/stats/fake-stats.service.ts` - исправлена синтаксическая ошибка

## 🎯 Результат

Все требования выполнены:
- ✅ Уведомления отправляются при любом изменении баланса
- ✅ Отображается причина изменения
- ✅ Показываются старый и новый баланс
- ✅ Указывается дата и время
- ✅ Обновляется fake-статистика
- ✅ Транзакции записываются в БД
- ✅ Обработка ошибок не ломает операции
- ✅ Уведомления не дублируются

## 🔄 Следующие шаги

Рекомендуется протестировать:
1. Изменение баланса через админ-панель (пополнение/списание)
2. Выполнение заданий пользователями
3. Реферальные бонусы
4. Заявки на вывод средств
5. Проверить, что уведомления приходят корректно
6. Проверить логи в `balance_logs` через админ-панель

## 📞 Поддержка

Если возникают проблемы:
1. Проверьте логи backend: `npm run start:dev` или `docker logs tg-backend`
2. Убедитесь, что бот имеет доступ к отправке сообщений пользователю
3. Проверьте, что `TELEGRAM_BOT_TOKEN` установлен корректно

