# 🔧 Исправление ошибки на сервере

## Проблема была исправлена

**Ошибка:** `sh: nest: not found` - NestJS CLI не был установлен в production режиме

**Решение:** Dockerfile обновлён - теперь все зависимости устанавливаются перед сборкой, а dev-зависимости удаляются после

---

## Команды для обновления на сервере

```bash
# 1. Обновить репозиторий
cd /root/tg-main
git pull origin sync/cleanup/2025-10-29

# 2. Остановить контейнеры
docker compose down

# 3. Пересобрать и запустить
docker compose up -d --build

# 4. Проверить логи
docker compose logs -f backend
```

---

## Полная последовательность команд (скопируйте и выполните на сервере):

```bash
cd /root/tg-main && \
git pull origin sync/cleanup/2025-10-29 && \
docker compose down && \
docker compose up -d --build
```

---

## После успешного запуска:

```bash
# Проверить статус контейнеров
docker compose ps

# Проверить логи backend
docker compose logs -f backend

# Проверить логи всех сервисов
docker compose logs -f

# Запустить миграции БД
docker compose exec backend npm run migration:run
```

---

## Если всё ОК, продолжить настройку SSL:

```bash
# Настроить SSL сертификаты
./setup-ssl.sh

# Установить автозапуск
sudo ./scripts/install-systemd-service.sh
```

