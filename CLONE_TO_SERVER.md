# 🖥️ Клонирование репозитория на сервер

## Команды для клонирования на сервер 79.174.93.115

### Вариант 1: Клонировать конкретную ветку (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Подключиться к серверу
ssh root@79.174.93.115

# 2. Перейти в нужную директорию
cd /root

# 3. Клонировать репозиторий с конкретной веткой
git clone -b sync/cleanup/2025-10-29 https://github.com/KantaevMarat/telegram-bot-admin.git tg-main

# 4. Перейти в директорию проекта
cd tg-main

# 5. Проверить текущую ветку
git branch

# 6. Проверить статус
git status
```

### Вариант 2: Клонировать все ветки, затем переключиться

```bash
# 1. Подключиться к серверу
ssh root@79.174.93.115

# 2. Клонировать репозиторий
cd /root
git clone https://github.com/KantaevMarat/telegram-bot-admin.git tg-main

# 3. Перейти в директорию
cd tg-main

# 4. Переключиться на нужную ветку
git checkout sync/cleanup/2025-10-29

# 5. Проверить
git branch
```

### Вариант 3: Если репозиторий уже склонирован (обновление)

```bash
# 1. Подключиться к серверу
ssh root@79.174.93.115

# 2. Перейти в директорию проекта
cd /root/tg-main

# 3. Получить все ветки
git fetch origin

# 4. Переключиться на нужную ветку
git checkout sync/cleanup/2025-10-29

# 5. Обновить до последней версии
git pull origin sync/cleanup/2025-10-29
```

---

## Полная последовательность для первого деплоя

```bash
# 1. Подключиться к серверу
ssh root@79.174.93.115

# 2. Перейти в /root
cd /root

# 3. Клонировать репозиторий с нужной веткой
git clone -b sync/cleanup/2025-10-29 https://github.com/KantaevMarat/telegram-bot-admin.git tg-main

# 4. Перейти в директорию проекта
cd tg-main

# 5. Проверить что ветка правильная
git branch

# 6. Создать .env из шаблона
cp env.production .env

# 7. Отредактировать .env (изменить пароли и JWT_SECRET)
nano .env

# 8. Сделать скрипты исполняемыми
chmod +x scripts/*.sh setup-ssl.sh

# 9. Запустить деплой
./scripts/deploy.sh

# 10. Настроить SSL
./setup-ssl.sh

# 11. Установить автозапуск
sudo ./scripts/install-systemd-service.sh
```

---

## Проверка после клонирования

```bash
# Проверить текущую ветку
git branch

# Должно показать:
# * sync/cleanup/2025-10-29

# Проверить последний commit
git log --oneline -1

# Должно показать:
# 1f24087 chore: clean repository - remove temporary files and update .gitignore

# Проверить наличие важных файлов
ls -la docker-compose.yml
ls -la env.production
ls -la scripts/deploy.sh
ls -la nginx/nginx.conf
```

---

## Если нужно обновить репозиторий на сервере

```bash
# На сервере
cd /root/tg-main
git fetch origin
git pull origin sync/cleanup/2025-10-29
```

---

## Полезные команды

```bash
# Посмотреть все ветки
git branch -a

# Посмотреть удаленные ветки
git branch -r

# Переключиться на другую ветку
git checkout main

# Посмотреть историю коммитов
git log --oneline -10
```

