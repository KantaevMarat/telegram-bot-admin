# 🔍 Проверка логов Backend

## Backend контейнер перезапускается

**Проблема:** `STATUS: Restarting (1)`

---

## Команды для диагностики:

### 1. Посмотреть логи backend:

```bash
docker compose logs backend
```

или с отслеживанием:

```bash
docker compose logs -f backend
```

### 2. Посмотреть последние 50 строк:

```bash
docker compose logs --tail=50 backend
```

### 3. Проверить статус всех контейнеров:

```bash
docker compose ps
```

### 4. Посмотреть логи всех сервисов:

```bash
docker compose logs
```

---

## Возможные причины:

1. **Ошибка подключения к базе данных**
   - Проверить переменные окружения в `.env`
   - Убедиться что `DB_HOST=postgres` (не localhost)

2. **Ошибка TypeScript компиляции**
   - Не все модули были добавлены в git

3. **Отсутствует JWT_SECRET или другие обязательные переменные**
   - Проверить `.env` файл

4. **Порт уже занят**
   - Backend пытается использовать порт 3000

---

## Быстрая диагностика (выполните на сервере):

```bash
# Посмотреть логи
docker compose logs --tail=100 backend

# Если нужно перезапустить
docker compose restart backend

# Или пересобрать
docker compose up -d --build backend
```

