# 🔧 Исправление проблемы: Cannot find module '/app/dist/main'

## Проблема

Backend контейнер не может найти собранные файлы в `/app/dist/main`

## Причина

В multi-stage build папка `dist` из stage BUILD не копируется правильно в production stage.

---

## Команды для диагностики на сервере:

### 1. Проверить что внутри контейнера backend:

```bash
docker compose run --rm backend ls -la /app/
docker compose run --rm backend ls -la /app/dist/ 2>/dev/null || echo "dist не существует"
```

### 2. Посмотреть логи сборки backend:

```bash
docker compose build backend 2>&1 | tail -50
```

### 3. Пересобрать backend с выводом всех логов:

```bash
docker compose build --no-cache --progress=plain backend
```

---

## Возможные решения:

### Решение 1: Проверить что сборка прошла успешно

```bash
# Пересобрать с полными логами
docker compose build --no-cache backend

# Если сборка успешна, перезапустить
docker compose up -d backend
```

### Решение 2: Временно вернуться к простому Dockerfile (без multi-stage)

Если multi-stage не работает, можно использовать более простой подход.

