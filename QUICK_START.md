# 🚀 Быстрый старт развертывания на сервере

## 📋 Что было создано

✅ **SSH ключ** с паролем `627846`  
✅ **Скрипты установки** для сервера (включая универсальные для чистого сервера)  
✅ **Автоматические скрипты** развертывания  
✅ **Подробная документация**

## ⚠️ Чистый сервер?

Если на вашем сервере нет `apt`, `git` и других пакетных менеджеров, используйте:
- `server-setup-universal.sh` - автоматически определит дистрибутив
- `server-setup-minimal.sh` - минимальная установка (только curl/wget)

См. `CLEAN_SERVER_SETUP.md` для подробностей.

## 🎯 Быстрый старт (3 шага)

### Шаг 1: Отправьте файлы в Git

```powershell
git add server-setup.sh server-setup-quick.sh deploy.ps1 copy-to-server.ps1 deploy-to-server.md git-setup.md
git commit -m "Add server deployment scripts"
git push
```

### Шаг 2: Развертывание на сервере

**Вариант A: Автоматическое (рекомендуется)**
```powershell
.\deploy.ps1
```

**Вариант B: Ручное**
```powershell
# 1. Копирование файлов
.\copy-to-server.ps1

# 2. Подключение к серверу
ssh -i "$env:USERPROFILE\.ssh\id_ed25519_new" root@79.174.93.115
# Пароль: 627846

# 3. На сервере запустите установку
bash server-setup-quick.sh https://github.com/KantaevMarat/telegram-bot-admin.git
```

### Шаг 3: Настройка и запуск

После установки на сервере:

```bash
# 1. Настройте переменные окружения
cd ~/projects/tg-main
nano .env.production

# 2. Запустите проект
docker compose -f docker-compose.production.yml up -d

# 3. Проверьте статус
docker compose -f docker-compose.production.yml ps
```

## 📝 Важные настройки в .env.production

Обязательно настройте:
- `TELEGRAM_BOT_TOKEN` - токен бота
- `TELEGRAM_WEBHOOK_URL` - URL для webhook
- `ADMIN_BOT_TOKEN` - токен админ-бота
- `JWT_SECRET` - секретный ключ (сгенерируйте случайную строку)
- `DB_PASSWORD` - пароль БД
- `REDIS_PASSWORD` - пароль Redis
- `VITE_API_URL` - URL вашего API

## 🔧 Полезные команды

```bash
# Просмотр логов
docker compose -f docker-compose.production.yml logs -f

# Перезапуск
docker compose -f docker-compose.production.yml restart

# Остановка
docker compose -f docker-compose.production.yml down

# Обновление проекта
cd ~/projects/tg-main
git pull
docker compose -f docker-compose.production.yml build
docker compose -f docker-compose.production.yml up -d
```

## 📚 Подробная документация

- `deploy-to-server.md` - Полная инструкция по развертыванию
- `git-setup.md` - Настройка Git

## ⚠️ Важно

1. **SSH порт**: Убедитесь, что порт 22 открыт на сервере
2. **Файрвол**: Откройте порты 3000, 8080, 9000, 9001
3. **Безопасность**: Измените все пароли по умолчанию в .env.production

## 🆘 Проблемы?

Если не можете подключиться по SSH:
1. Проверьте, открыт ли порт 22
2. Используйте файл `ssh_support_request.txt` для обращения в поддержку
3. Проверьте правильность SSH ключа

