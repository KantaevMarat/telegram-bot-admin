# 🚀 Финальная инструкция по развертыванию всех изменений

## ✅ Реализованные функции:

### 1. **Механика задержки перед подтверждением задания** ⏱️
- Добавлено поле `min_completion_time` (минуты) в задания
- Бот проверяет время и блокирует преждевременное подтверждение
- UI для настройки в админке

### 2. **Система модерации заданий** 📋
- Задания с `task_type='manual'` идут на модерацию
- API для одобрения/отклонения заданий
- Пользователь получает уведомление о проверке

### 3. **Страница модерации в админке** 🛠️
- Новая страница `/moderation`
- Фильтры по статусу (на проверке, в процессе, завершено, отклонено)
- Поиск по username/ID пользователя
- Кнопки одобрения/отклонения

### 4. **Подсчет и просмотр медиафайлов в чатах** 📸
- Счетчик медиафайлов рядом с именем пользователя
- Кнопка фильтрации "Медиа" в чате
- Показывает только сообщения с медиа

### 5. **Исправлено отображение пользователей** 👤
- Правильный формат: `@username` или `Имя Фамилия`
- Второй уровень информации (ID или имя)
- Последовательное отображение во всех страницах

---

## 📦 Измененные файлы:

### Backend:
1. `backend/src/entities/task.entity.ts`
2. `backend/src/modules/tasks/dto/create-task.dto.ts`
3. `backend/src/modules/tasks/tasks.service.ts`
4. `backend/src/modules/tasks/tasks.module.ts`
5. `backend/src/modules/tasks/tasks.controller.ts`
6. `backend/src/modules/bot/bot.service.ts`
7. `backend/src/modules/messages/messages.service.ts`
8. `backend/add-min-completion-time-column.js` (новый файл миграции)

### Frontend:
1. `frontend/src/pages/TasksPage.tsx`
2. `frontend/src/pages/ButtonsPage.tsx`
3. `frontend/src/pages/ChatsPage.tsx`
4. `frontend/src/pages/ModerationPage.tsx` (новая страница)
5. `frontend/src/api/client.ts`
6. `frontend/src/App.tsx`
7. `frontend/src/components/Layout.tsx`

---

## 🔧 Развертывание (выполните по порядку):

### Шаг 1: Миграция БД
```bash
ssh -i "C:\Users\Марат\.ssh\telegram_bot_admin" root@79.174.93.115
# Пароль: qOAkAE8P1VRGou71

cd ~/telegram-bot-admin/backend
node add-min-completion-time-column.js
```

**Ожидаемый результат:**
```
✅ Connected to database
✅ Column min_completion_time added successfully
```

---

### Шаг 2: Обновление Backend

**На локальной машине:**
```powershell
cd C:\Games\tg-main

# Копируем все измененные файлы
scp -i "C:\Users\Марат\.ssh\telegram_bot_admin" backend/src/entities/task.entity.ts root@79.174.93.115:~/telegram-bot-admin/backend/src/entities/
scp -i "C:\Users\Марат\.ssh\telegram_bot_admin" backend/src/modules/tasks/dto/create-task.dto.ts root@79.174.93.115:~/telegram-bot-admin/backend/src/modules/tasks/dto/
scp -i "C:\Users\Марат\.ssh\telegram_bot_admin" backend/src/modules/tasks/tasks.service.ts root@79.174.93.115:~/telegram-bot-admin/backend/src/modules/tasks/
scp -i "C:\Users\Марат\.ssh\telegram_bot_admin" backend/src/modules/tasks/tasks.module.ts root@79.174.93.115:~/telegram-bot-admin/backend/src/modules/tasks/
scp -i "C:\Users\Марат\.ssh\telegram_bot_admin" backend/src/modules/tasks/tasks.controller.ts root@79.174.93.115:~/telegram-bot-admin/backend/src/modules/tasks/
scp -i "C:\Users\Марат\.ssh\telegram_bot_admin" backend/src/modules/bot/bot.service.ts root@79.174.93.115:~/telegram-bot-admin/backend/src/modules/bot/
scp -i "C:\Users\Марат\.ssh\telegram_bot_admin" backend/src/modules/messages/messages.service.ts root@79.174.93.115:~/telegram-bot-admin/backend/src/modules/messages/
```

**На сервере через SSH:**
```bash
# Перезапуск backend
cd ~/telegram-bot-admin
docker-compose restart backend

# Проверка логов
docker logs tg-backend --tail 50

# Проверка, что API работает
curl -s http://localhost:3001/health || echo "Backend не запущен!"
```

---

### Шаг 3: Обновление Frontend

**На локальной машине:**
```powershell
cd C:\Games\tg-main\frontend

# Пересборка
npm run build

# Копирование на сервер
cd ..
scp -r -i "C:\Users\Марат\.ssh\telegram_bot_admin" frontend/dist/* root@79.174.93.115:~/telegram-bot-admin/frontend/dist/
```

**На сервере через SSH:**
```bash
# Обновление файлов в контейнере
docker cp ~/telegram-bot-admin/frontend/dist/. tg-frontend:/usr/share/nginx/html/

# Перезагрузка Nginx
docker exec tg-frontend nginx -s reload

echo "✅ Frontend обновлен!"
```

---

## 🧪 Проверка работоспособности:

### 1. Проверка API (на сервере):
```bash
# Проверка эндпоинта модерации
curl -s "http://localhost:3001/api/admin/tasks/moderation/pending" | jq
```

### 2. Проверка админки (в браузере):
1. Откройте https://app.marranasuete.ru
2. Войдите в админку
3. Проверьте новый пункт меню **"Модерация"** (иконка часов)
4. Зайдите в **"Задания"** → убедитесь, что есть поле **"⏱️ Минимальное время выполнения"**
5. Зайдите в **"Кнопки"** → проверьте подсказку для **callback data**
6. Зайдите в **"Чаты"** → проверьте:
   - Отображение `@username` или `Имя Фамилия`
   - Счетчик медиафайлов (иконка 📷 + число)
   - Кнопку фильтрации "Медиа" в открытом чате

### 3. Проверка бота (Telegram):
1. Начните выполнение задания с `min_completion_time > 0`
2. Попробуйте сразу подтвердить выполнение
3. Бот должен показать: **"⏳ Подождите немного! Кнопка подтверждения станет доступна через: X мин"**

---

## 🎯 Что ожидать:

### В боте:
- ⏱️ Задержка перед подтверждением заданий
- 📋 Уведомление "Задание отправлено на проверку!" для task_type='manual'
- ✅ Одобренные задания начисляют награду

### В админке:
- 🕒 Страница **Модерация** со списком заданий на проверке
- 🔍 Поиск по username/ID пользователей
- 📊 Статистика: на проверке, в процессе, к выплате
- 👤 Правильное отображение пользователей (@username или Имя Фамилия)
- 📸 Счетчик медиафайлов в чатах
- 🖼️ Фильтр "Медиа" для просмотра только медиафайлов

---

## ❗ Возможные ошибки и решения:

### Ошибка: "Column min_completion_time does not exist"
**Решение:** Запустите миграцию `node add-min-completion-time-column.js`

### Ошибка: "Cannot read property 'username' of undefined"
**Решение:** Перезапустите backend: `docker-compose restart backend`

### Ошибка: 404 на `/moderation`
**Решение:** 
1. Убедитесь, что frontend пересобран: `npm run build`
2. Скопируйте файлы на сервер
3. Обновите nginx: `docker exec tg-frontend nginx -s reload`

---

## 📞 Поддержка:

Если возникли проблемы:
1. Проверьте логи backend: `docker logs tg-backend --tail 100`
2. Проверьте логи frontend: `docker logs tg-frontend --tail 50`
3. Убедитесь, что все файлы скопированы правильно

---

**Удачи с развертыванием! 🚀**

