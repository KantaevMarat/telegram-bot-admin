# Компоненты управления кнопками Telegram

## Структура компонентов

### ButtonEditor
Главный компонент редактора кнопок. Объединяет все части:
- Выбор режима кнопки
- Конфигурация режима
- Редактирование разделов и подкнопок
- Превью
- Валидация
- Экспорт/импорт JSON
- Тестирование

### ButtonModeSelector
Компонент для выбора режима кнопки (command, text, media, url, function, submenu).

### ModeConfigPanel
Компонент для настройки параметров выбранного режима.

### SectionsEditor
Компонент для управления разделами и подкнопками (для режима submenu).

### ButtonPreview
Компонент превью кнопки с поддержкой мобильного/десктоп вида и светлой/тёмной темы.

### ButtonTestPanel
Компонент для тестирования кнопок.

## Использование

```tsx
import ButtonEditor from './components/buttons/ButtonEditor';
import { ButtonConfig } from './types/button.types';

const config: ButtonConfig = {
  id: 'btn_001',
  label: 'Каталог',
  mode: 'submenu',
  visibility: 'both',
  permissions: ['all'],
  sections: [...]
};

<ButtonEditor
  config={config}
  existingConfigs={[]}
  onSave={async (config) => {
    // Сохранение конфигурации
  }}
  onClose={() => {}}
  onTest={async (config) => {
    // Тестирование кнопки
  }}
/>
```

## Режимы кнопок

1. **command** - Выполнить команду бота
2. **text** - Отправить текстовое сообщение
3. **media** - Отправить медиа (фото, видео, аудио, документ)
4. **url** - Открыть URL
5. **function** - Выполнить webhook, скрипт или внутреннюю функцию
6. **submenu** - Открыть подменю с разделами и подкнопками

## Валидация

- Название: 1-64 символа
- callback_data: до 64 байт, уникальность
- URL: валидный формат http/https
- Медиа: указание типа и источника

## JSON формат

Конфигурация экспортируется в JSON по схеме:
```json
{
  "id": "btn_001",
  "label": "Каталог",
  "icon": "🛍️",
  "color": "#3B82F6",
  "visibility": "both",
  "permissions": ["all"],
  "mode": "submenu",
  "sections": [...]
}
```

