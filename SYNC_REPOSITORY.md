# 🔄 Синхронизация репозитория с локальным проектом

## План действий для полной синхронизации

### Вариант 1: Обновить текущую ветку (sync/cleanup/2025-10-29)

```bash
# 1. Удалить лишние файлы из репозитория
git rm --cached .env.production.temp
git rm --cached .cursor/worktrees.json

# 2. Обновить .gitignore
git add .gitignore

# 3. Закоммитить изменения
git commit -m "chore: remove temporary files and update .gitignore"

# 4. Отправить в репозиторий
git push origin sync/cleanup/2025-10-29
```

### Вариант 2: Заменить main ветку чистой версией (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Переключиться на main
git checkout main

# 2. Получить последние изменения
git pull origin main

# 3. Слить изменения из sync/cleanup/2025-10-29
git merge sync/cleanup/2025-10-29

# 4. Или заменить main полностью (если нужна полная замена)
git reset --hard sync/cleanup/2025-10-29

# 5. Отправить в репозиторий
git push origin main --force  # ВНИМАНИЕ: force push!
```

### Вариант 3: Создать новую чистую ветку main

```bash
# 1. Создать новую ветку от sync/cleanup/2025-10-29
git checkout -b main-clean sync/cleanup/2025-10-29

# 2. Удалить все лишние файлы
git rm --cached .env.production.temp
git rm --cached .cursor/worktrees.json

# 3. Обновить .gitignore
git add .gitignore
git commit -m "chore: clean repository from temporary files"

# 4. Отправить новую ветку
git push origin main-clean

# 5. В GitHub сделать main-clean основной веткой
```

---

## Текущий статус

✅ Удалены из индекса:
- `.env.production.temp`
- `.cursor/worktrees.json`

✅ Обновлен `.gitignore`:
- Добавлен `.cursor/`
- Добавлен `.env.production.temp`

---

## Рекомендация

**Используйте Вариант 1** - просто обновите текущую ветку, это безопаснее.

Если хотите сделать main ветку чистой, используйте Вариант 2, но будьте осторожны с `--force` push!

