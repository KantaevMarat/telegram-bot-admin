# 🚀 Quick Start Guide

Быстрый запуск на production сервере за 5 минут.

---

## Предварительные требования

- ✅ Сервер Ubuntu 20.04+ с root доступом
- ✅ IP: 79.174.93.115
- ✅ Домены настроены:
  - api.marranasuete.ru → 79.174.93.115
  - app.marranasuete.ru → 79.174.93.115

---

## 1️⃣ Подключение к серверу

```bash
ssh root@79.174.93.115
```

---

## 2️⃣ Подготовка сервера (первый раз)

```bash
# Установка Docker, Git, настройка firewall
curl -fsSL https://raw.githubusercontent.com/YOUR_REPO/main/scripts/prepare-server.sh | sudo bash
```

**Или вручную:**

```bash
# Docker
curl -fsSL https://get.docker.com | sudo bash

# Docker Compose
sudo apt install docker-compose-plugin -y

# Git
sudo apt install git -y

# Firewall
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw --force enable
```

---

## 3️⃣ Клонирование проекта

```bash
cd /root
git clone <YOUR_REPOSITORY_URL> tg-main
cd tg-main
```

---

## 4️⃣ Настройка переменных окружения

```bash
# Копировать шаблон
cp env.production .env

# Редактировать
nano .env
```

**Обязательно измените:**

```bash
# JWT Secret (минимум 32 символа) - ОБЯЗАТЕЛЬНО!
JWT_SECRET=ваш-случайный-секретный-ключ-минимум-32-символа

# Пароли
REDIS_PASSWORD=ваш_надежный_пароль_redis
MINIO_SECRET_KEY=ваш_надежный_пароль_minio
DB_PASSWORD=ваш_надежный_пароль_postgres
```

**Сохранить:** `Ctrl+X`, `Y`, `Enter`

---

## 5️⃣ Запуск приложения

```bash
# Сделать скрипты исполняемыми
chmod +x scripts/*.sh setup-ssl.sh

# Запустить деплой
./scripts/deploy.sh
```

Ожидайте ~2-3 минуты. Скрипт:
- ✅ Создаст необходимые директории
- ✅ Запустит все контейнеры
- ✅ Проверит здоровье сервисов
- ✅ Выполнит миграции БД

---

## 6️⃣ Настройка SSL сертификатов

```bash
./setup-ssl.sh
```

Скрипт автоматически:
- ✅ Получит SSL сертификаты для обоих доменов
- ✅ Настроит HTTPS
- ✅ Настроит автообновление

---

## 7️⃣ Настройка автозапуска

```bash
sudo ./scripts/install-systemd-service.sh
```

Приложение будет автоматически запускаться при перезагрузке сервера.

---

## ✅ Проверка

### Проверить контейнеры
```bash
docker compose ps
```
Все должны быть **Up** или **healthy**.

### Проверить доступность
- **API**: https://api.marranasuete.ru/api/docs
- **Frontend**: https://app.marranasuete.ru

### Проверить Telegram ботов
Отправьте `/start`:
- @thtgmoneybot (клиентский)
- @lrtelegram_mgbot (админ)

---

## 📝 Полезные команды

```bash
# Просмотр логов
./scripts/logs.sh

# Проверка статуса
./scripts/status.sh

# Обновление приложения
./scripts/update.sh

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Запуск
docker compose up -d
```

---

## 🔄 Обновление после изменений

```bash
cd /root/tg-main
git pull
docker compose down
docker compose up -d --build
```

Или просто:
```bash
./scripts/update.sh
```

---

## 🚨 Если что-то пошло не так

### Просмотр логов
```bash
docker compose logs -f
```

### Перезапуск конкретного сервиса
```bash
docker compose restart backend
docker compose restart frontend
docker compose restart nginx
```

### Полная перезагрузка
```bash
docker compose down
docker compose up -d --build
```

### Проверка конфигурации Nginx
```bash
docker compose exec nginx nginx -t
```

---

## 📞 Дополнительная помощь

- **Полная документация**: `README.md`
- **Детальный деплой**: `DEPLOYMENT.md`
- **Все команды**: `SUMMARY.md`

---

## ⏱️ Итого

- **Подготовка сервера**: ~5 минут
- **Деплой приложения**: ~3 минуты
- **Настройка SSL**: ~2 минуты
- **Итого**: ~10 минут ⚡

**Готово! Приложение запущено в production! 🎉**

