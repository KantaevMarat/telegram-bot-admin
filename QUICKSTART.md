# 🚀 Быстрый старт

## 📦 Что вам нужно

- Docker & Docker Compose (рекомендуется)
- Node.js 18+ (если без Docker)
- PostgreSQL, Redis, MinIO (если без Docker)

## ⚡ 5 шагов до запуска

### 1️⃣ Настройте боты

**Автоматически (рекомендуется):**

Linux/macOS:
```bash
chmod +x setup-bots.sh
./setup-bots.sh
```

Windows PowerShell:
```powershell
.\setup-bots.ps1
```

**Вручную:**
1. Скопируйте `env.example.txt` в `.env`
2. Вставьте токены:
   - `TELEGRAM_BOT_TOKEN=8330680651:AAErG1_zzA0aX4_O7s-aaQlcCseLF7i8cIE`
   - `ADMIN_BOT_TOKEN=8339258038:AAHd4UGAxiDxI57TBi5_REn1GBOg1n50cro`

### 2️⃣ Настройте домен (для Web App)

Для локальной разработки используйте ngrok:
```bash
ngrok http 5173
```

Скопируйте HTTPS URL в `.env`:
```env
TELEGRAM_WEB_APP_URL=https://abc123.ngrok.io
```

### 3️⃣ Запустите проект

**С Docker:**
```bash
docker-compose up -d
```

**Без Docker:**
```bash
npm install
cd backend && npm install && npm run start:dev
cd ../frontend && npm install && npm run dev
```

### 4️⃣ Добавьте себя как администратора

Узнайте свой Telegram ID:
- Отправьте `/start` клиентскому боту
- Или используйте [@userinfobot](https://t.me/userinfobot)

Добавьте себя:
```bash
# С Docker
docker exec -it tg-backend npm run add-admin YOUR_TG_ID password123

# Без Docker
cd backend
npm run add-admin YOUR_TG_ID password123
```

### 5️⃣ Тестируйте!

**Клиентский бот:**
1. Найдите своего бота в Telegram (username можно узнать из скрипта setup-bots)
2. Отправьте `/start`
3. Попробуйте кнопки меню

**Админ-бот:**
1. Найдите админ-бота в Telegram
2. Отправьте `/start`
3. Нажмите "📊 Открыть админ-панель"

## 📱 Ваши боты

- **Клиентский бот:** токен `8330680651:AAErG1_zzA0aX4_O7s-aaQlcCseLF7i8cIE`
- **Админ-бот:** токен `8339258038:AAHd4UGAxiDxI57TBi5_REn1GBOg1n50cro`

## 🔍 Проверка статуса

```bash
# Логи
docker-compose logs -f backend

# Проверка ботов через API
curl "https://api.telegram.org/bot8330680651:AAErG1_zzA0aX4_O7s-aaQlcCseLF7i8cIE/getMe"
curl "https://api.telegram.org/bot8339258038:AAHd4UGAxiDxI57TBi5_REn1GBOg1n50cro/getMe"
```

## ❓ Проблемы?

1. **Админ-бот не отвечает** → Проверьте токен в `.env` и перезапустите backend
2. **Веб-интерфейс не открывается** → Убедитесь что TELEGRAM_WEB_APP_URL это HTTPS
3. **"Нет доступа к админ-панели"** → Добавьте себя как администратора (шаг 4)

## 📚 Подробная документация

- [TELEGRAM_SETUP.md](./TELEGRAM_SETUP.md) - полная инструкция по настройке
- [README.md](./README.md) - документация проекта

## 🎯 Что дальше?

1. Создайте несколько заданий через админ-панель
2. Настройте обязательные каналы для подписки
3. Настройте сценарии автоответов
4. Настройте рассылки
5. Добавьте других администраторов

---

💡 **Совет:** Используйте `docker-compose logs -f backend` чтобы следить за работой ботов в реальном времени

