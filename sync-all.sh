#!/bin/bash
# 🔄 Полная синхронизация с сервером - запустите на сервере

set -e  # Остановиться при ошибке

echo "🚀 Начинаем полную синхронизацию..."
echo ""

# Шаг 1: Обновление кода
echo "📥 Шаг 1/6: Получение последних изменений из Git..."
cd ~/telegram-bot-admin
git pull origin sync/cleanup/2025-10-29
echo "✅ Код обновлён"
echo ""

# Шаг 2: Пересборка фронтенда
echo "🏗️  Шаг 2/7: Пересборка фронтенда..."
docker compose -f docker-compose.lightweight.yml build --no-cache frontend
echo "✅ Фронтенд пересобран"
echo ""

# Шаг 3: Перезапуск контейнеров
echo "🔄 Шаг 3/7: Перезапуск всех контейнеров..."
docker compose -f docker-compose.lightweight.yml down
docker compose -f docker-compose.lightweight.yml up -d
echo "⏳ Ожидание запуска сервисов (30 секунд)..."
sleep 30
echo "✅ Контейнеры перезапущены"
echo ""

# Шаг 4: Копирование файлов в контейнер (ПОСЛЕ запуска)
echo "📦 Шаг 4/7: Копирование файлов в контейнер..."
docker cp backend/dist/. tg-backend:/app/dist/
docker cp backend/init-settings-simple.js tg-backend:/app/init-settings-simple.js

# Копирование фронтенда в контейнер (если пересборка не помогла)
if [ -d "frontend/dist" ]; then
  echo "📦 Копирование фронтенда в контейнер..."
  docker cp frontend/dist/. tg-frontend:/usr/share/nginx/html/ 2>/dev/null || echo "⚠️  Контейнер фронтенда еще не запущен, будет скопировано после перезапуска"
fi

echo "✅ Файлы скопированы"
echo ""

# Шаг 5: Проверка статуса
echo "🔍 Шаг 5/7: Проверка статуса контейнеров..."
docker compose -f docker-compose.lightweight.yml ps
echo ""

# Шаг 6: Инициализация настроек
echo "⚙️  Шаг 6/7: Инициализация настроек..."
# Ждем еще немного, чтобы база данных точно была готова
sleep 5
docker compose -f docker-compose.lightweight.yml exec -T backend node init-settings-simple.js || echo "⚠️  Предупреждение: Не удалось инициализировать настройки (возможно, уже инициализированы)"
echo ""

# Шаг 7: Проверка логов
echo "📋 Шаг 7/7: Последние логи backend..."
docker compose -f docker-compose.lightweight.yml logs backend | tail -30
echo ""

echo "🎉 Синхронизация завершена!"
echo ""
echo "📊 Проверьте:"
echo "   1. Админка: http://89.35.55.254:8080"
echo "   2. API: http://89.35.55.254:3000/api/docs"
echo ""
echo "🔧 Количество настроек в базе:"
sudo -u postgres psql -d u3315562_developer -t -c "SELECT COUNT(*) FROM settings;" 2>/dev/null || echo "⚠️  Не удалось проверить"
echo ""
echo "✨ Готово!"

